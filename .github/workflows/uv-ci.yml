name: UV CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true # 开启缓存，下次运行只需几秒钟

      - name: Set up Python
        run: uv python install

      - name: Install dependencies
        run: uv sync --all-extras --dev

      - name: Verify Environment
        run: |
          uv run python -c "import pandas, numpy, cvxpy, loguru; print('🚀 All Quant Stacks Loaded!')"
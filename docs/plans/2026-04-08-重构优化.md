# 重构优化 实施计划

> **给Claude的提示：** 必需子技能：使用 superpowers:executing-plans 逐任务实施此计划。

**目标：** 拆分过大文件、提取重复 pool 解析与数据加载代码为公共工具，提升可维护性，不改变公共接口。

**架构：** 分两阶段实施：Phase 1 先提取 `cli/_loader.py` 消除 4 处重复的 pool+DataProvider 初始化；Phase 2 将 `cli/trades.py`（1321行）拆成 3 个文件，将 `data_provider/unified_factor_builder.py`（600行）的清洗方法提取到 `DataCleanerMixin`。

**技术栈：** Python 3.12、polars、typer、rich、pytest

---

## 任务 1：提取 `cli/_loader.py`（消除 4 处 pool 解析重复）

**文件：**
- 创建：`src/alpha_factory/cli/_loader.py`
- 修改：`src/alpha_factory/cli/eval_core.py`（删除 `_get_pool_universe`、`_load_data`，改用 `_loader`）
- 修改：`src/alpha_factory/cli/opt.py`（删除 `_resolve_pool`，改用 `_loader`）
- 修改：`src/alpha_factory/cli/opt2.py`（删除 `_resolve_pool`，改用 `_loader`）
- 修改：`src/alpha_factory/cli/trades.py`（删除 `_resolve_pool`，改用 `_loader`）
- 测试：`tests/test_cli_loader.py`

**步骤 1：编写测试（先失败）**

```python
# tests/test_cli_loader.py
import pytest
from alpha_factory.cli._loader import resolve_pool


def test_resolve_pool_known():
    pool = resolve_pool("main_small_pool")
    assert pool.name == "main_small_pool"


def test_resolve_pool_unknown_raises():
    with pytest.raises(ValueError, match="未知股票池"):
        resolve_pool("nonexistent_pool")


def test_resolve_pool_error_message_lists_valid_options():
    with pytest.raises(ValueError) as exc_info:
        resolve_pool("bad_pool")
    assert "main_small_pool" in str(exc_info.value)
```

**步骤 2：运行测试，确认失败**

```bash
uv run pytest tests/test_cli_loader.py -v
```
预期：`ModuleNotFoundError: No module named 'alpha_factory.cli._loader'`

**步骤 3：创建 `src/alpha_factory/cli/_loader.py`**

```python
"""_loader.py — cli 层共享的 pool 解析 + DataProvider 加载工具。

替代 opt.py / opt2.py / eval_core.py / trades.py 中重复的
_resolve_pool / _get_pool_universe 逻辑。
"""

from __future__ import annotations

from typing import Optional

import polars as pl
from loguru import logger

from alpha_factory.cli.utils import PoolUniverseEnum
from alpha_factory.data_provider.data_provider import DataProvider
from alpha_factory.data_provider.pool import PoolUniverse


def resolve_pool(pool_name: str) -> PoolUniverse:
    """将股票池名称字符串解析为 PoolUniverse 实例。

    遍历 PoolUniverseEnum，找到 name 匹配的成员并实例化返回。
    若未找到则抛出 ValueError（包含所有可选值）。
    """
    for member in PoolUniverseEnum:
        instance = member.value()
        if instance.name == pool_name:
            return instance
    valid = ", ".join(m.value().name for m in PoolUniverseEnum)
    raise ValueError(f"未知股票池 {pool_name!r}，可选值: {valid}")


def load_pool_lf(
    pool: PoolUniverse,
    exprs: list[str],
    start_date: str,
    end_date: Optional[str],
    actions: Optional[list] = None,
) -> pl.LazyFrame:
    """初始化 DataProvider 并加载股票池数据，返回 LazyFrame。

    Args:
        pool: PoolUniverse 实例（由 resolve_pool 获取）
        exprs: 因子表达式列表，格式 "name=expression"
        start_date: 起始日期 YYYYMMDD
        end_date: 结束日期 YYYYMMDD（None 表示至最新）
        actions: 可选的预处理 action 列表（来自 StrategyConfig.build_actions()）

    Returns:
        pl.LazyFrame: 包含因子列和标签列的惰性数据框
    """
    logger.info(
        f"加载数据: pool={pool.name}, start={start_date}, end={end_date}, "
        f"factors={len(exprs)}"
    )
    dp = DataProvider()
    kwargs: dict = {"exprs": exprs}
    if actions is not None:
        kwargs["actions"] = actions
    return dp.load_pool_data(pool, start_date, end_date, **kwargs)
```

**步骤 4：运行测试，确认通过**

```bash
uv run pytest tests/test_cli_loader.py -v
```
预期：3 个测试全部通过。

**步骤 5：更新 `eval_core.py`**

删除头部的 `_get_pool_universe` 和 `_load_data` 两个函数，在文件顶部 import 中添加：
```python
from alpha_factory.cli._loader import load_pool_lf, resolve_pool
```

将 `run_eval_pipeline` 中调用 `_get_pool_universe(config.pool)` 改为 `resolve_pool(config.pool)`；
将 `_load_data(...)` 的调用替换为 `load_pool_lf(pool_universe, exprs_for_loader, actual_start, actual_end)`。

（`_collect_factors` 函数保留不动。）

**步骤 6：更新 `opt.py`**

删除 `_resolve_pool` 函数（约第 43-57 行），在文件顶部 import 中添加：
```python
from alpha_factory.cli._loader import resolve_pool
```
全文搜索 `_resolve_pool(` 替换为 `resolve_pool(`。

**步骤 7：更新 `opt2.py`**

同 opt.py：删除 `_resolve_pool`，改用 `from alpha_factory.cli._loader import resolve_pool`。

**步骤 8：更新 `trades.py`**

删除 `_resolve_pool` 函数，改用：
```python
from alpha_factory.cli._loader import resolve_pool
```
全文搜索 `_resolve_pool(` 替换为 `resolve_pool(`。

**步骤 9：运行全量测试**

```bash
uv run pytest --maxfail=3 -q
```
预期：158 个测试全部通过（加上新增 3 个 = 161 个通过）。

**步骤 10：lint 检查**

```bash
uv run ruff check .
```
预期：All checks passed!

**步骤 11：提交**

```bash
git add src/alpha_factory/cli/_loader.py \
        src/alpha_factory/cli/eval_core.py \
        src/alpha_factory/cli/opt.py \
        src/alpha_factory/cli/opt2.py \
        src/alpha_factory/cli/trades.py \
        tests/test_cli_loader.py
git commit -m "refactor: 提取 cli/_loader.py，消除 4 处重复的 pool 解析与 DataProvider 加载逻辑"
```

---

## 任务 2：拆分 `cli/trades.py` — 提取 SHAP 可视化到 `_trades_viz.py`

**文件：**
- 创建：`src/alpha_factory/cli/_trades_viz.py`
- 修改：`src/alpha_factory/cli/trades.py`（删除 SHAP 可视化函数，改从 `_trades_viz` 导入）

trades.py 中以下 7 个函数移入 `_trades_viz.py`：
- `_MPL_CONFIGURED`（全局变量）
- `_configure_mpl`
- `_shap_to_base64`
- `_detect_shap_threshold`
- `_shap_dependence_grid`
- `_shap_heatmap_by_time`
- `_shap_interaction_plots`
- `_shap_decision_plots`

`trades.py` 的 import 块增加：
```python
from alpha_factory.cli._trades_viz import (
    _configure_mpl,
    _shap_to_base64,
    _detect_shap_threshold,
    _shap_dependence_grid,
    _shap_heatmap_by_time,
    _shap_interaction_plots,
    _shap_decision_plots,
)
```

**步骤 1：创建 `_trades_viz.py`**

将上列函数从 `trades.py` 剪切到新文件。新文件顶部：
```python
"""_trades_viz.py — trades 命令的 SHAP 可视化辅助函数。"""

from __future__ import annotations

import base64
import io
from typing import Optional

_MPL_CONFIGURED = False
```
保留原来的全部 import（matplotlib、shap、numpy 等）以及所有函数体内容。

**步骤 2：更新 `trades.py` 的 import**

在 `trades.py` 顶部增加从 `_trades_viz` 的导入，删除已移走的函数体和 `_MPL_CONFIGURED` 全局变量定义。

**步骤 3：运行并验证测试**

```bash
uv run pytest --maxfail=3 -q
```
预期：全部通过（函数已通过 import 重新暴露，行为不变）。

**步骤 4：提交**

```bash
git add src/alpha_factory/cli/_trades_viz.py src/alpha_factory/cli/trades.py
git commit -m "refactor: 将 trades.py 的 SHAP 可视化函数提取到 _trades_viz.py"
```

---

## 任务 3：拆分 `cli/trades.py` — 提取 HTML 报告到 `_trades_html.py`

**文件：**
- 创建：`src/alpha_factory/cli/_trades_html.py`
- 修改：`src/alpha_factory/cli/trades.py`（删除 HTML 生成函数，改从 `_trades_html` 导入）

trades.py 中以下 2 个函数移入 `_trades_html.py`：
- `_build_diagnosis_html`
- `_generate_html_report`

`trades.py` 的 import 块增加：
```python
from alpha_factory.cli._trades_html import (
    _build_diagnosis_html,
    _generate_html_report,
)
```

**步骤 1：创建 `_trades_html.py`**

新文件顶部：
```python
"""_trades_html.py — trades 命令的 HTML 报告生成逻辑。"""

from __future__ import annotations

import base64
import datetime
from pathlib import Path
from typing import Optional

import polars as pl
from rich.console import Console

from alpha_factory.cli._trades_viz import (
    _configure_mpl,
    _shap_to_base64,
    _detect_shap_threshold,
    _shap_dependence_grid,
    _shap_heatmap_by_time,
    _shap_interaction_plots,
    _shap_decision_plots,
)
from alpha_factory.config.strategy import StrategyConfig

console = Console()
```

将 `_build_diagnosis_html` 和 `_generate_html_report` 函数体完整剪切到此文件。

**步骤 2：更新 `trades.py`**

删除 `_build_diagnosis_html` 和 `_generate_html_report` 函数体，添加：
```python
from alpha_factory.cli._trades_html import _build_diagnosis_html, _generate_html_report
```

**步骤 3：运行并验证测试**

```bash
uv run pytest --maxfail=3 -q
```
预期：全部通过。

**步骤 4：lint 检查**

```bash
uv run ruff check .
```
预期：All checks passed!

**步骤 5：提交**

```bash
git add src/alpha_factory/cli/_trades_html.py src/alpha_factory/cli/trades.py
git commit -m "refactor: 将 trades.py 的 HTML 报告生成提取到 _trades_html.py"
```

---

## 任务 4：拆分 `data_provider/unified_factor_builder.py` — 提取清洗 Mixin

**文件：**
- 创建：`src/alpha_factory/data_provider/_data_cleaner.py`
- 修改：`src/alpha_factory/data_provider/unified_factor_builder.py`
- 测试：`tests/test_data_cleaner.py`

以下 8 个方法从 `UnifiedFactorBuilder` 移入 `DataCleanerMixin`：
- `_op_clean_daily`
- `_op_clean_adj`
- `_op_clean_basic`
- `_op_clean_limit`
- `_op_clean_suspend`
- `_op_clean_st`
- `_op_clean_disclosure`
- `_op_process_indicators`

**步骤 1：编写测试（先失败）**

```python
# tests/test_data_cleaner.py
from alpha_factory.data_provider._data_cleaner import DataCleanerMixin
from alpha_factory.data_provider.unified_factor_builder import UnifiedFactorBuilder


def test_data_cleaner_mixin_exists():
    """DataCleanerMixin 可以独立导入。"""
    assert DataCleanerMixin is not None


def test_unified_factor_builder_inherits_mixin():
    """UnifiedFactorBuilder 继承 DataCleanerMixin，8 个清洗方法可访问。"""
    builder_methods = dir(UnifiedFactorBuilder)
    for method in [
        "_op_clean_daily",
        "_op_clean_adj",
        "_op_clean_basic",
        "_op_clean_limit",
        "_op_clean_suspend",
        "_op_clean_st",
        "_op_clean_disclosure",
        "_op_process_indicators",
    ]:
        assert method in builder_methods, f"缺少方法: {method}"
```

**步骤 2：运行测试，确认失败**

```bash
uv run pytest tests/test_data_cleaner.py -v
```
预期：`ModuleNotFoundError: No module named 'alpha_factory.data_provider._data_cleaner'`

**步骤 3：创建 `_data_cleaner.py`**

新文件顶部：
```python
"""_data_cleaner.py — UnifiedFactorBuilder 的数据清洗 Mixin。

将原 unified_factor_builder.py 中所有 _op_clean_* 和 _op_process_indicators
方法提取到此 Mixin，使 UnifiedFactorBuilder 的单文件代码量降至合理范围。
"""

from __future__ import annotations

from datetime import date
from typing import TYPE_CHECKING, List

import polars as pl

if TYPE_CHECKING:
    pass


class DataCleanerMixin:
    """数据清洗操作 Mixin，提供各类原始数据的清洗方法。

    设计约束：
    - 每个 _op_clean_* 方法必须是纯函数（仅依赖入参和 self.cache，无副作用）。
    - 返回值统一为 pl.LazyFrame。
    - 调用方（UnifiedFactorBuilder）通过继承获取所有方法。
    """
```

然后将 `unified_factor_builder.py` 中以下方法的完整代码剪切粘贴到 class 体内（保持所有依赖 import 在文件顶部）：
`_op_clean_daily`、`_op_clean_adj`、`_op_clean_basic`、`_op_clean_limit`、`_op_clean_suspend`、`_op_clean_st`、`_op_clean_disclosure`、`_op_process_indicators`。

**步骤 4：更新 `unified_factor_builder.py`**

在顶部 import 处添加：
```python
from alpha_factory.data_provider._data_cleaner import DataCleanerMixin
```

将类声明从：
```python
class UnifiedFactorBuilder:
```
改为：
```python
class UnifiedFactorBuilder(DataCleanerMixin):
```

然后删除已移走的 8 个方法体。

**步骤 5：运行测试，确认通过**

```bash
uv run pytest tests/test_data_cleaner.py tests/test_unified_factor_builder.py -v
```
预期：全部通过。

**步骤 6：运行全量测试 + lint**

```bash
uv run pytest --maxfail=3 -q && uv run ruff check .
```
预期：全部通过，无 lint 错误。

**步骤 7：提交**

```bash
git add src/alpha_factory/data_provider/_data_cleaner.py \
        src/alpha_factory/data_provider/unified_factor_builder.py \
        tests/test_data_cleaner.py
git commit -m "refactor: 提取 DataCleanerMixin 到 _data_cleaner.py，精简 unified_factor_builder.py"
```

---

## 任务 5：验收与汇总

**步骤 1：全量测试**

```bash
uv run pytest -v 2>&1 | tail -20
```
预期：全部原有测试通过（≥158），新增测试（test_cli_loader.py 3个 + test_data_cleaner.py 2个）也通过。

**步骤 2：lint 检查**

```bash
uv run ruff check .
```
预期：All checks passed!

**步骤 3：确认文件大小压缩效果**

```bash
wc -l src/alpha_factory/cli/trades.py \
       src/alpha_factory/cli/_trades_viz.py \
       src/alpha_factory/cli/_trades_html.py \
       src/alpha_factory/data_provider/unified_factor_builder.py \
       src/alpha_factory/data_provider/_data_cleaner.py
```
预期：
- `trades.py` 从 1321 行降至 ~350 行
- `unified_factor_builder.py` 从 600 行降至 ~280 行

**步骤 4：最终提交（如有遗留改动）**

```bash
git status
# 若有未提交改动：
git add -A && git commit -m "chore: 重构验收 — 全量测试 pass，lint clean"
```

---

## 附录：关键约束提醒

1. **不改变公共接口**：所有 `__init__.py` 不需要更新，`_loader.py`、`_trades_viz.py`、`_trades_html.py`、`_data_cleaner.py` 均以 `_` 开头，属于内部模块。
2. **imports 迁移**：每个新文件的顶部 import 必须完整（从 trades.py 移走函数时，连带 `io`、`base64`、`datetime` 等也要带走）。
3. **pre-commit hooks**：提交时会自动运行 pytest，确保每步提交前测试通过。
4. **逐步提交**：每个任务独立提交，不要将多个任务合并提交，便于必要时回滚。

# Alpha-Factory 2019-2024 因子挖掘完整指南

## 📋 快速概览

本文档介绍如何使用 Alpha-Factory 平台在 2019-2024 年历史数据上进行自动化因子挖掘。整个流程分为三个主要步骤：

1. **数据准备** (`sync_data_2019_2024.py`) - 从 Tushare 同步 6 年历史数据
2. **因子挖掘** (`mine_factors_2019_2024.py`) - 使用 DEAP 遗传编程自动发现因子
3. **效果评估** - 对挖掘出的因子进行效果验证

---

## 🚀 快速开始（3 步）

### 第 1 步：检查环境

```bash
# 确保已安装依赖
pip install -r requirements.txt

# 验证 Tushare 配置（在 .env 中）
echo "TUSHARE_TOKEN=your_token" > .env

# 验证项目结构
ls -la data/ alpha/ output/ config/
```

### 第 2 步：同步数据（可选，如果已有数据可跳过）

```bash
# 同步 2019-2024 全年数据（约 1-2 小时）
python scripts/sync_data_2019_2024.py \
    --start-year 2019 \
    --end-year 2024 \
    --mode full \
    --resume

# 或仅同步 2024 年（快速测试）
python scripts/sync_data_2019_2024.py \
    --start-year 2024 \
    --end-year 2024
```

### 第 3 步：启动因子挖掘

```bash
# 完整挖掘（建议先用单年份测试）
python scripts/mine_factors_2019_2024.py \
    --start-year 2024 \
    --end-year 2024 \
    --n-gen 20 \
    --n-pop 500

# 如果第一次运行成功，再扩展到全年份
python scripts/mine_factors_2019_2024.py \
    --start-year 2019 \
    --end-year 2024 \
    --n-gen 15 \
    --n-pop 400
```

---

## 📊 脚本详细说明

### 1. 数据同步脚本 (`sync_data_2019_2024.py`)

**功能**：从 Tushare 获取 2019-2024 年全市场日线数据，进行 ETL 清洗，存储为 Parquet 格式。

**典型命令**：

```bash
# 全量同步（首次运行）
python scripts/sync_data_2019_2024.py \
    --start-year 2019 \
    --end-year 2024 \
    --mode full \
    --vip

# 覆盖已有数据重新同步
python scripts/sync_data_2019_2024.py \
    --start-year 2024 \
    --end-year 2024 \
    --overwrite

# 不使用 VIP 限流（速度较慢）
python scripts/sync_data_2019_2024.py \
    --start-year 2024 \
    --end-year 2024 \
    --no-vip
```

**参数说明**：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--start-year` | 2019 | 同步起始年份 |
| `--end-year` | 2024 | 同步结束年份 |
| `--mode` | full | 同步模式：full (全量) 或 incremental (增量) |
| `--resume` | True | 支持断点续传 |
| `--overwrite` | False | 覆盖已有数据 |
| `--vip` | True | 使用 Tushare VIP 限流策略 |
| `--save-summary` | True | 保存摘要报告 |

**输出**：

- **数据位置**：`data/warehouse/unified_factors/2019.parquet` ... `2024.parquet`
- **摘要报告**：`output/reports/sync_summary_YYYYMMDD_HHMMSS.txt`
- **检查点**：`output/logs/sync_checkpoint.json`（支持断点续传）

**性能参考**：

| 场景 | 耗时 | 数据量 |
|------|------|--------|
| 单年份 (2024) | 5-10 分钟 | ~130 万行 |
| 两年份 (2023-2024) | 10-20 分钟 | ~260 万行 |
| 全 6 年份 | 45-90 分钟 | ~780 万行 |

**常见问题**：

- **Q: 首次运行很慢**  
  A: Tushare API 限流，建议使用 VIP 账户加速

- **Q: 如何只同步缺失的日期？**  
  A: 使用 `--mode incremental --resume`

- **Q: 数据如何验证？**  
  A: 脚本自动检查行数、列数、空值率

---

### 2. 因子挖掘脚本 (`mine_factors_2019_2024.py`)

**功能**：使用 DEAP 遗传编程在 2019-2024 年数据上自动发现因子。每个年份独立进化，避免数据泄露。

**核心工作流**：

```
年份 1 (2019)     年份 2 (2020)         ...        年份 6 (2024)
       ↓                ↓                            ↓
  数据准备      数据准备                    数据准备
       ↓                ↓                            ↓
  因子挖掘 ↔   因子挖掘 ↔   ...   ↔        因子挖掘
  (20 代)     (20 代)             (20 代)
       ↓                ↓                            ↓
  最优因子      最优因子                    最优因子
 (hof 100)     (hof 100)                   (hof 100)
```

**典型命令**：

```bash
# 快速测试（单年份，5 代，100 种群，约 30 分钟）
python scripts/mine_factors_2019_2024.py \
    --start-year 2024 \
    --end-year 2024 \
    --n-gen 5 \
    --n-pop 100

# 常规挖掘（单年份，20 代，500 种群，约 2-3 小时）
python scripts/mine_factors_2019_2024.py \
    --start-year 2024 \
    --end-year 2024 \
    --n-gen 20 \
    --n-pop 500

# 深度优化（两年份，30 代，500 种群，约 6-8 小时）
python scripts/mine_factors_2019_2024.py \
    --start-year 2023 \
    --end-year 2024 \
    --n-gen 30 \
    --n-pop 500

# 全年份挖掘（6 年份，15 代，400 种群，约 12-16 小时）
python scripts/mine_factors_2019_2024.py \
    --start-year 2019 \
    --end-year 2024 \
    --n-gen 15 \
    --n-pop 400
```

**参数说明**：

| 参数 | 默认值 | 范围 | 说明 |
|------|--------|------|------|
| `--start-year` | 2019 | 2015-2025 | 挖掘起始年份 |
| `--end-year` | 2024 | 2015-2025 | 挖掘结束年份 |
| `--n-gen` | 20 | 5-50 | 进化代数（越多越好，但耗时↑） |
| `--n-pop` | 500 | 100-1000 | 初始种群大小（越大多样性↑） |
| `--batch-size` | 50 | 20-100 | 批处理大小（影响内存使用） |
| `--label` | RETURN_OO_1 | str | 目标标签列名 |
| `--overwrite-data` | False | - | 覆盖缓存数据 |
| `--save-summary` | True | - | 保存摘要报告 |

**输出**：

- **名人堂因子**：`output/gp/2024/best_hof.pkl` (100 个最优因子)
- **种群备份**：`output/gp/2024/exprs_000.pkl`, `exprs_001.pkl` ... (每代备份)
- **适应度缓存**：`output/gp/2024/fitness_cache.pkl` (加速恢复)
- **摘要报告**：`output/reports/mining_summary_*.txt`

**进化参数详解**：

- **n_gen (进化代数)**
  - 更多代数 → 更好收敛（可能找到更优因子）
  - 但每多 1 代 ≈ +3-5 分钟耗时
  - **建议**：轻量 (5-10)，常规 (15-25)，深度 (30-50)

- **n_pop (种群大小)**
  - 更大种群 → 更好多样性（防止局部最优）
  - 但每增 100 ≈ +10-15% 耗时
  - **建议**：轻量 (100-200)，常规 (300-500)，深度 (500-1000)

- **batch_size (批处理大小)**
  - 每次一起计算多少个表达式的适应度
  - 更大 batch ↔ 更快 BUT 更多内存
  - **建议**：内存 < 8GB 用 20-30，8-16GB 用 40-60，> 16GB 用 80-100

**中断与恢复**：

脚本支持自动断点续传：
1. 如果执行被中断（如按 Ctrl+C），已计算的适应度会被缓存
2. 重新运行相同命令，脚本会自动加载缓存，跳过已计算部分
3. 如需完全重新挖掘，加上 `--overwrite-data`

```bash
# 原命令中断了，再运行一遍（自动恢复）
python scripts/mine_factors_2019_2024.py \
    --start-year 2024 \
    --end-year 2024 \
    --n-gen 20 \
    --n-pop 500

# 如果想完全重新来过
python scripts/mine_factors_2019_2024.py \
    --start-year 2024 \
    --end-year 2024 \
    --n-gen 20 \
    --n-pop 500 \
    --overwrite-data
```

---

## 📈 性能预估

### 单机 4 核 CPU 典型耗时

| 配置 | 年份数 | n_gen | n_pop | 预计耗时 |
|------|--------|-------|-------|---------|
| 轻量测试 | 1 | 5 | 100 | 30 分钟 |
| 快速验证 | 1 | 10 | 200 | 1 小时 |
| 常规挖掘 | 1 | 20 | 500 | 2-3 小时 |
| 深度优化 | 1 | 30 | 500 | 3-4 小时 |
| 两年份 | 2 | 20 | 400 | 4-6 小时 |
| 全 6 年份 | 6 | 15 | 300 | 8-12 小时 |

### 优化建议

1. **减少耗时**
   - 降低 `n_gen` (从 20 → 15)
   - 降低 `n_pop` (从 500 → 300)
   - 增加 `batch_size` (从 50 → 100)

2. **提高质量**
   - 增加 `n_gen` (从 15 → 25)
   - 增加 `n_pop` (从 300 → 500)
   - 多年份挖掘 (而非单年份)

3. **内存不足**
   - 减少 `batch_size` (从 50 → 20)
   - 减少 `n_pop` (从 500 → 200)

---

## 🔄 完整工作流示例

### 场景 A：快速原型验证（1 小时）

```bash
# 1. 同步 2024 年数据（如果没有）
python scripts/sync_data_2019_2024.py \
    --start-year 2024 \
    --end-year 2024

# 2. 轻量挖掘 2024 年
python scripts/mine_factors_2019_2024.py \
    --start-year 2024 \
    --end-year 2024 \
    --n-gen 5 \
    --n-pop 100 \
    --batch-size 30

# 3. 查看最优因子
python scripts/inspect_halloffame.py --year 2024
```

### 场景 B：标准挖掘流程（6-8 小时）

```bash
# 1. 同步 2019-2024 年数据（如果没有）
python scripts/sync_data_2019_2024.py \
    --start-year 2019 \
    --end-year 2024 \
    --mode full

# 2. 常规挖掘 2019-2024
python scripts/mine_factors_2019_2024.py \
    --start-year 2019 \
    --end-year 2024 \
    --n-gen 15 \
    --n-pop 400 \
    --batch-size 50

# 3. 生成评估报告
python scripts/evaluate_factors.py \
    --hof output/gp/best_hof_all.pkl \
    --method rankic

# 4. 多因子回测
python scripts/backtest_factors.py \
    --factors output/gp/best_hof_all.pkl \
    --start-date 20190101 \
    --end-date 20241231
```

### 场景 C：深度研究优化（24 小时+）

```bash
# 1. 同步全部数据
python scripts/sync_data_2019_2024.py \
    --start-year 2015 \
    --end-year 2024 \
    --mode full

# 2. 深度挖掘各年份
for year in {2019..2024}; do
    python scripts/mine_factors_2019_2024.py \
        --start-year $year \
        --end-year $year \
        --n-gen 30 \
        --n-pop 500 \
        --label RETURN_OO_1
done

# 3. 合并各年份最优因子
python scripts/merge_hofs.py \
    --years 2019 2020 2021 2022 2023 2024 \
    --output output/gp/merged_best_hof.pkl

# 4. 综合评估与回测
python scripts/evaluate_factors.py \
    --hof output/gp/merged_best_hof.pkl \
    --method rankic \
    --rolling-window 252
```

---

## 🛠️ 故障排查

### 常见问题

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| `TUSHARE_TOKEN 未设置` | .env 文件缺失或配置错误 | `echo "TUSHARE_TOKEN=xxx" > .env` |
| 数据文件不存在 | 未运行数据同步脚本 | `python scripts/sync_data_2019_2024.py` |
| 内存溢出 (OOM) | 参数过大 | 减少 `n_pop`，增加 `batch_size` |
| 进化缓慢 | 数据质量差或超参数不佳 | 增加 `n_gen`，检查数据 |
| 中断后无法恢复 | 检查点丢失 | 使用 `--overwrite-data` 重新开始 |

### 调试模式

```bash
# 启用详细日志
export LOGURU_LEVEL=DEBUG

# 单步测试（仅处理 2024 年 1 周数据）
python scripts/mine_factors_2019_2024.py \
    --start-year 2024 \
    --end-year 2024 \
    --n-gen 2 \
    --n-pop 50

# 查看日志输出
tail -f output/logs/*.log
```

---

## 📦 输出文件结构

```
output/
├── gp/
│   ├── 2019/
│   │   ├── data_cache/
│   │   │   └── labeled_RETURN_OO_1.parquet  # 标签数据缓存
│   │   ├── fitness_cache.pkl                 # 适应度缓存
│   │   ├── exprs_000.pkl, exprs_001.pkl ...  # 每代种群
│   │   ├── best_hof.pkl                      # 最优因子 100 个
│   │   └── 2019_result.pkl                   # 完整结果
│   ├── 2020/
│   │   └── ...
│   └── 2024/
│       └── ...
├── logs/
│   ├── build_unified_2025.log
│   ├── sync_2025_resume.log
│   └── sync_checkpoint.json
├── reports/
│   ├── sync_summary_20260123_120000.txt
│   ├── mining_summary_20260123_150000.txt
│   └── evaluation_report_20260123_160000.txt
└── models/
    └── (LightGBM 模型)
```

---

## 📚 下一步

1. **查看最优因子**
   ```bash
   python scripts/inspect_halloffame.py --year 2024 --top 10
   ```

2. **评估因子效果**
   ```bash
   python scripts/evaluate_factors.py \
       --year 2024 \
       --method rankic \
       --window 252
   ```

3. **多因子回测**
   ```bash
   python scripts/backtest_factors.py \
       --factors output/gp/2024/best_hof.pkl \
       --start-date 20240101 \
       --end-date 20241231
   ```

4. **生成最终报告**
   ```bash
   python scripts/generate_report.py \
       --hof output/gp/2024/best_hof.pkl \
       --output output/reports/final_report.html
   ```

---

## 📞 支持与反馈

- 查看完整 API 文档：`alpha/gp/factors_generator.py` 的 docstrings
- 检查示例脚本：`examples/sync_data_example.py`
- 查看测试用例：`tests/test_*.py`

---

**祝挖掘愉快！🚀**
